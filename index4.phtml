<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h2>Задание:</h2>
    <p> Дан массив $mas = [4, 5, 1, 4, 7, 8, 15, 6, 71, 45, 2]; сделайте алгоритм и найдите сумму между
        минимальным и максимальными значениями. Не используя готовые функции. <br>
        1. Используя цикл. <br>
        2. Используйте один цикл, в один проход. <br>
    </p>
    <h2>Решение:</h2>
    <p>
    <pre><code>
    //1. Используем цикл
    $mas = [4, 5, 1, 4, 7, 8, 15, 6, 71, 45, 2];
    $max = $mas[0]; //сделаем максимум первым элементом массива
    $min = $mas[0]; //сделаем минимумом второй элемент массива
    $sum1 = 0; //переменная для результата
    for ($i = 1; $i < count($mas); $i++) {
        if ($mas[$i] > $max) {
            $max = $mas[$i];
        }
    }
    for ($i = 1; $i < count($mas); $i++) {
        if ($mas[$i] < $min) {
            $min = $mas[$i];
        }
    }
    $sum1 = $max + $min;
    echo ($sum1 . "\n");

    //2. Работа в 1 проход
    $sum2 = 0; //переменная для результата
//цикл для поиска максимального и минимального числа. Начинаем проход с 2 второго числа так так первое
//используется для инициализации
    for ($i = 1; $i < count($mas); $i++) {
        //находим максимальное число
        if ($mas[$i] > $max) {
            $max = $mas[$i];
        }
        ;
        //находим минимальное число.
        if ($mas[$i] < $min) {
            $min = $mas[$i];
        }
        ;
    }
    $sum2 = $max + $min;
    echo $sum2;
</code></pre>
    </p>
    <h2>Результат выполнения кода:</h2>
    <?php
    /*4*. Дан массив $mas = [4, 5, 1, 4, 7, 8, 15, 6, 71, 45, 2]; сделайте алгоритм и найдите сумму между
    минимальным и максимальными значениями. Не используя готовые функции. Используя цикл.
    ** Используйте один цикл, в один проход. */
    //1. Используем цикл
    $mas = [4, 5, 1, 4, 7, 8, 15, 6, 71, 45, 2];
    $max = $mas[0]; //сделаем максимум первым элементом массива
    $min = $mas[0]; //сделаем минимумом второй элемент массива
    $sum1 = 0; //переменная для результата
    for ($i = 1; $i < count($mas); $i++) {
        if ($mas[$i] > $max) {
            $max = $mas[$i];
        }
    }
    for ($i = 1; $i < count($mas); $i++) {
        if ($mas[$i] < $min) {
            $min = $mas[$i];
        }
    }
    $sum1 = $max + $min;
    echo ($sum1 . "\n");

    //2. Работа в 1 проход
    $sum2 = 0; //переменная для результата
//цикл для поиска максимального и минимального числа. Начинаем проход с 2 второго числа так так первое
//используется для инициализации
    for ($i = 1; $i < count($mas); $i++) {
        //находим максимальное число
        if ($mas[$i] > $max) {
            $max = $mas[$i];
        }
        ;
        //находим минимальное число.
        if ($mas[$i] < $min) {
            $min = $mas[$i];
        }
        ;
    }
    $sum2 = $max + $min;
    echo $sum2;
    ?>
</body>

</html>